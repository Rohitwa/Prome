"""ProMem agent — small Python module that pushes local tracker.db segments
to ProMem's cloud upload endpoint.

Phase 4b — designed to be packaged via NSIS installer + auto-update via
manifest URL. Runs under Windows Task Scheduler every ~5 min.
"""

__version__ = "0.1.6"
